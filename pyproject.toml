[project]
name = "reeds"
version = "0.0.0"
requires-python = ">=3.11"

[dependency-groups]
framework-comparison = [
  "gamspy",
  "highspy",
  "linopy",
  "numpy",
  "pyomo",
  "pyoptinterface[highs]",
  "xarray",
  "xpress==9.7.*",
]
